compile:

requires fuse library and headers installed on the system
make

usage:

./fusehalt /path/to/mountat /path/to/halt_file -f

to halt IO:
 echo 1 > /path/to/halt_file

to unhalt IO:
 echo 0 > /path/to/halt_file

unhalt to exit cleanly
